[pytest]
addopts = -v -s -x
testpaths = tests

python_files =
    test_*.py
    *_test.py

markers =
    unit: Marks tests as fast, isolated unit tests (no GUI)
    integration: Marks tests that involve GUI interactions or external dependencies
    snapshot: Marks tests that use snapshot testing to verify GUI output or state